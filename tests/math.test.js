test("test somme math", () => {
  expect(1 + 2).toBe(3);
});

describe("Users tests suites", () => {
  test("test users", () => {
    expect(1 + 2).toBe(3);
  });

  test("test users 2", () => {
    expect(1 + 2).toBe(3);
  });
});
